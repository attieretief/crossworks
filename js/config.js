/* Public settings. No secrets here — the Worker holds those.
   Set `api` to the Worker's URL once it is deployed (see worker/README.md).
   Until it is set, the newsletter form and the editor stay dormant. */
window.CROSSWORKS = {
  api: ''
};
